/*var nombre = prompt('¿Cual es tu nombre?');
document.write("Hola " + nombre + " Saludos desde Javascript")*/

/*var monto1 = parseInt(prompt("ingresar cantidad:")) 
var monto2 = parseInt(prompt("ingresar cantidad:"))  


function sumar(monto1, monto2){

    var resultado = monto1 + monto2;
    document.write(resultado +'</br>');
}

sumar(monto1, monto2);

function restar(monto1, monto2){

    var resultado = monto1 - monto2;
    document.write(resultado +'</br>');
}

restar(monto1, monto2);

function dividir(monto1, monto2){

    var resultado = monto1 / monto2;
    document.write(resultado +'</br>');
}

dividir(monto1, monto2);

function multiplicar(monto1, monto2){

    var resultado = monto1 * monto2;
    document.write(resultado +'</br>');
}

multiplicar(monto1, monto2);*/